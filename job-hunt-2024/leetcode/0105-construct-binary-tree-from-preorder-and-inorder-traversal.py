# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution:
    def buildTree(self, preorder: List[int], inorder: List[int]) -> Optional[TreeNode]:
        # Time: O(n)
        # Space: O(n)

        # Preorder = ชิดซ้าย
        # Inorder = เรียงเลข
        # Fuck this shit how am I supposed to pull this question off?????

        root = TreeNode()

